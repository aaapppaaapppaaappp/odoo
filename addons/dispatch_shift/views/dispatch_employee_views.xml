<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit hr.employee form to add dispatch buttons -->
    <record id="view_employee_form_inherit_dispatch" model="ir.ui.view">
        <field name="name">hr.employee.form.dispatch</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_dispatch_applications"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-calendar-check-o">
                    <field name="dispatch_application_count" widget="statinfo"
                           string="Dispatch"/>
                </button>
            </xpath>
            <xpath expr="//header" position="inside">
                <button name="action_send_dispatch_link" type="object"
                        string="Send Dispatch Link" class="btn-secondary"
                        icon="fa-paper-plane"/>
            </xpath>
        </field>
    </record>

    <!-- Action to open hr.employee from dispatch menu -->
    <record id="dispatch_employee_action" model="ir.actions.act_window">
        <field name="name">Employees</field>
        <field name="res_model">hr.employee</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
